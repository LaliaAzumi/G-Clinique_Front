export interface Secretary {
  id: string;
  firstName: string;
  lastName: string;
  username: string;
  email: string;
  phone: string;
  assignedService: string; // ex: Cardiologie, Urgences, Accueil
}